---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ics-proxy
spec:
  refreshInterval: 5m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: ics-proxy-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        on-call.url: "{{ .JSM_ICS_URL }}"
  dataFrom:
    - extract:
        key: ics-on-call
