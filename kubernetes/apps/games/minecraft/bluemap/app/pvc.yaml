---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bluemap-shared
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 50Gi
  storageClassName: ceph-filesystem
