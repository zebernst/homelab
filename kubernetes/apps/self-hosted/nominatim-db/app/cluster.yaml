# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/postgresql.cnpg.io/cluster_v1.json
---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: nominatim-db
spec:
  instances: 1
  imageName: ghcr.io/cloudnative-pg/postgis:16-3.4
  primaryUpdateStrategy: unsupervised
  enableSuperuserAccess: true
  superuserSecret:
    name: nominatim-db-secret
  storage:
    size: 400Gi
    storageClass: ceph-block
  resources:
    requests:
      cpu: "2"
      memory: 24Gi
    limits:
      memory: 28Gi
  affinity:
    # Prefer same node as Nominatim API/flatnode (not UI / import Jobs).
    additionalPodAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchLabels:
                app.kubernetes.io/name: nominatim
                app.kubernetes.io/controller: nominatim
            topologyKey: kubernetes.io/hostname
  postgresql:
    parameters:
      max_connections: "100"
      shared_buffers: 2GB
      maintenance_work_mem: 4GB
      autovacuum_work_mem: 1GB
      effective_cache_size: 12GB
      work_mem: 50MB
      synchronous_commit: "off"
      checkpoint_timeout: 10min
      checkpoint_completion_target: "0.9"
      max_wal_size: 1GB
  # Historical: physically cloned from embedded Nominatim (nominatim-legacy).
  # Bootstrap is not re-run after create; kept so CNPG metrics default to the
  # nominatim database/owner. replica: removed after promote.
  bootstrap:
    pg_basebackup:
      database: nominatim
      owner: nominatim
      source: nominatim-legacy
  # Admission stub only — CNPG rejects bootstrap.pg_basebackup.source unless
  # this name exists. Not used for streaming (replica removed). Points at this
  # cluster's RW Service so nominatim-pg-source can be pruned.
  externalClusters:
    - name: nominatim-legacy
      connectionParameters:
        host: nominatim-db-rw.self-hosted.svc
        user: postgres
        dbname: postgres
  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: nominatim-db-backup
        serverName: nominatim-db-v1
  monitoring:
    enablePodMonitor: true
